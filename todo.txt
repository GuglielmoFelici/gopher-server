IMPORTANTE: HEADER GUARDS

Common:
    usare / sia in linux che in windows? "path/path\" funziona...
    usare const e volatile?
    Controllare il funzionamento di atexit
    Spostare definizioni da datatypes
    Mettere startup() subito prima del main loop?
    controllare tutti i sizeof e gli strlen
    verificare tutte le *alloc e free
    togliere tutti gli _ ???
    Rivedere datatypes
    controllare i return -1
    Purgare costanti
    indenta le syscalls in ||
    startup() fa troppe cose?
    lock in sendfile
    

Linux:
    SPOSTARE LINUX LOGGING IN HELPER FILE?
    Processo daemon linux:
        loggare con syslog invece di file
    LOGGING TUTTO SBAGLIATO

Windows:
    verificare chiusura di tutti gli handle
    Eliminare validateInput()?
    Windows Logger
    InstallDefaultSigHandlers
        evitabile socket per awake select?

Ballino:
    compara byte per byte
    non usare stringhe hardcoded
    tcp
    controlla ogni singola system call
    makefile
    testa file da più di 2 GB
